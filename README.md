# Dev (Run project localy)
test
git clone https://github.com/omarpepito/library-manager.git

cd library-manager
npm install
npm run dev


## git steps

git config --global user.email "omarmamani408@gmail.com"
git config --global user.name "omarpepito"

## comun git commands

first you have to create a branch form main branch

git checkout -b Name-of-your-branch-no-spaces-here
git add .
git commit -m "some text"
git push origin Name-of-your-branch-no-spaces-here --forece
